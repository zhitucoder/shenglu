# 音频识别程序 — 设计文档

## 1. 概述

开发一个音频识别程序：用户上传音频文件，程序自动提取音频原文并生成转写文件，基于原文生成总结报告，并支持用户就音频内容进行提问，程序基于原文回答。

### 1.1 核心功能

| 编号 | 功能 | 说明 |
|------|------|------|
| F1 | 音频转写 | 上传音频 → 提取原文，保存为转写文件，完成后提示转写耗时 |
| F2 | 总结报告 | 基于转写原文，自动生成结构化总结报告，可下载为 md |
| F3 | 智能问答 | 用户基于音频原文提问，程序结合原文上下文回答，问答历史存 MySQL |
| F4 | 历史记录 | 查看历史提交的音频、原文、总结报告，可继续问答 |
| F5 | 会话改名 | 用户可给音频 session 重命名 |
| F6 | 下载 | 转写原文与总结报告支持下载为 md 文件 |

### 1.2 技术选型（已确认）

| 层 | 选型 | 理由 |
|----|------|------|
| ASR 语音识别 | **Faster-Whisper**（本地运行） | 免费、离线可用、支持中英文，GPU 可加速，transformer/ctranslate2 引擎速度快于原始 Whisper |
| 大模型 LLM | **云 API**（DeepSeek，OpenAI 兼容接口） | 效果好、接入简单、按量付费；可切换任意 OpenAI 兼容服务 |
| 后端框架 | **FastAPI + Uvicorn** | 异步高性能、自动生成 API 文档，适合 Web 服务 |
| 关系数据库 | **MySQL 8.0**（本机 3306） | 存储问答历史，数据库 `shenglu`，连接 `pymysql` |
| 前端 | **原生 HTML/CSS/JS**（单页） | 无需构建工具，轻量，满足上传 + 聊天交互 |
| 环境 | Python 3.12（conda `shenglu`） | 独立环境，已创建并装好依赖 |

## 2. 系统架构

```
┌──────────────┐        ┌──────────────────────────────────────────────┐
│  浏览器前端    │  HTTP  │                FastAPI 后端                   │
│  (Web UI)    │ ──────► │                                              │
└──────────────┘        │  ┌───────────┐ ┌────────────┐ ┌────────────┐ │
                        │  │audio API  │ │report API  │ │ chat API   │ │
                        │  │+sessions  │ └─────┬──────┘ │+history    │ │
                        │  └─────┬─────┘       │        └─────┬──────┘ │
                        │        │             │               │       │
                        │  ┌─────▼─────┐ ┌─────▼──────┐ ┌─────▼─────┐ │
                        │  │ ASR 服务   │ │ LLM 服务   │ │ MySQL     │ │
                        │  │faster-     │ │DeepSeek API│ │chat_messages│ │
                        │  │whisper    │ │(OpenAI兼容) │ └───────────┘ │
                        │  └─────┬─────┘ └─────┬──────┘               │
                        │        │             │                        │
                        │  ┌─────▼─────────────▼─────────────┐         │
                        │  │      存储服务 (文件)               │         │
                        │  │ audio/YYYY-MM/  transcripts/      │         │
                        │  │ reports/        sessions/         │         │
                        │  └──────────────────────────────────┘         │
                        └──────────────────────────────────────────────┘
```

**处理流程：**

1. 用户上传音频 → 后端保存原始音频，按月份存放 `audio/YYYY-MM/{id}.{ext}`
2. 后台异步执行 ASR 转写（Faster-Whisper，繁体自动转简体）→ 生成转写原文文件 `transcripts/{id}.md`，记录转写耗时
3. 前端轮询转写进度 → 完成后再生成总结报告
4. 总结报告：LLM 读取转写原文 → 生成结构化报告 → 保存 `reports/{id}.md`
5. 问答：LLM 结合「转写原文全文 + 历史对话（MySQL 读取）」生成回答；问答历史写入 MySQL
6. 历史记录：前端从 `sessions/*.json` 列出所有会话，可打开继续查看/问答/改名/下载

## 3. 模块设计

### 3.1 ASR 转写服务 (`services/asr.py`)

**Faster-Whisper 技术原理**：开源语音识别库，是 OpenAI Whisper 的高性能重实现，兼容原版模型但快 4 倍、省内存 4 倍。核心优势来源：

1. **CTranslate2 推理引擎** — 面向 Transformer 的 C++ 优化引擎，替代原版 PyTorch 推理，运行时开销大幅降低
2. **INT8 量化（`compute_type="int8"`）** — 权重从 fp16 压到 8bit，显存占用减半以上，CPU/GPU 均可运行
3. **内置 Silero VAD 语音活动检测** — 先识别并跳过纯静音段，只对含人声片段转写，长音频显著提速
4. **分段并行解码** — 类似 Batch 的分段处理，提升吞吐

**显存占用与模型选型**（INT8 量化）：

| 模型 | 显存占用 | 说明 |
|------|---------|------|
| tiny / base | ~1GB | 轻量，CPU 可跑 |
| small | ~2GB | 快速 |
| **medium（默认）** | **~5GB** | 中文效果不错，12G 显存下充裕 ✅ |
| large-v3 | ~10GB | 效果最好，12G 显存偏紧，需配合 `beam_size=1` + `vad_filter=True` |

**实现要点：**

- 封装 `faster_whisper.WhisperModel(model, device="cuda", compute_type="int8")`
- 模型选择策略（可配置，默认 `medium`）：
  - 高精度：`large-v3`（需 GPU，显存偏紧）
  - 默认：`medium`（中文效果好、显存有余量）
  - 轻量：`small` / `base`（CPU 可用）
- 设备降级：GPU 不可用或显存不足时，自动回退到 CPU
- 转写参数：`beam_size=1`、`vad_filter=True` 降低显存与耗时
- 支持常见音频格式：`wav / mp3 / m4a / flac / ogg / aac`，通过 ffmpeg 解码
- 转写输出：**带时间戳的分段文本**，同时输出纯文本版本，供 LLM 与展示使用
- 长音频分段处理：按 chunk 转写后拼接，避免内存溢出

### 3.2 LLM 服务 (`services/llm.py`)

- 统一封装 OpenAI 兼容 Chat Completions 接口
- 默认 base_url：`https://api.deepseek.com`，模型 `deepseek-chat`
- 通过环境变量注入 API Key，**严禁写入代码/配置文件**
- 提供两个方法：
  - `generate_summary(transcript)` → 生成总结报告
  - `chat(transcript, history, question)` → 基于原文问答
- 上下文管理：转写原文可能超过模型上下文，按需做截断或分段摘要策略

### 3.3 存储服务 (`services/storage.py`)

| 目录 | 内容 |
|------|------|
| `data/audio/YYYY-MM/{id}.{ext}` | 上传的原始音频，按月份子目录存放 |
| `data/transcripts/{id}.md` | 转写原文（含时间戳） |
| `data/reports/{id}.md` | 生成的总结报告 |
| `data/sessions/{id}.json` | 每个音频的会话状态（名称、进度、文件路径、耗时） |

- 文件命名使用 UUID 作为 `id`
- 会话状态用 JSON 持久化，重启不丢失
- 提供会话列表、改名、下载路径查询等操作

### 3.4 MySQL 服务 (`services/db.py`)

- 使用 `pymysql` 连接本机 MySQL（`127.0.0.1:3306`）
- 数据库 `shenglu`，表 `chat_messages`，启动时自动建库建表
- 存储问答历史：`add_message()` 写入、`get_messages()` 按时间倒序读取最近 50 条
- 连接配置项：`SL_MYSQL_HOST / SL_MYSQL_PORT / SL_MYSQL_USER / SL_MYSQL_PASSWORD / SL_MYSQL_DB`

### 3.5 Web 前端 (`static/`)

单页应用，四个视图：

1. **历史记录区**：列出全部会话（名称/时间/文件名/状态/耗时），支持改名与打开
2. **上传区**：拖拽/选择音频文件，上传后显示转写进度条，完成后提示耗时
3. **原文/报告区**：转写完成自动展示原文；可一键「生成总结报告」，报告与原文 Tab 展示，均可下载为 md
4. **问答区**：聊天界面，从 MySQL 加载历史对话，可继续提问

## 4. 接口设计

### REST API

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/audio` | 上传音频文件，返回 `{id}`，异步启动转写 |
| GET | `/api/audio/{id}` | 查询转写状态与结果（含进度、文本、耗时、名称） |
| PATCH | `/api/audio/{id}` | 会话改名（body: `{"name":"..."}`） |
| GET | `/api/audio/{id}/transcript/download` | 下载转写原文 md 文件 |
| GET | `/api/audio/{id}/report/download` | 下载总结报告 md 文件 |
| POST | `/api/audio/{id}/report` | 触发生成总结报告 |
| GET | `/api/audio/{id}/report` | 获取总结报告内容 |
| POST | `/api/audio/{id}/chat` | 发送问题，返回 LLM 回答（body: `{question}`） |
| GET | `/api/audio/{id}/chat` | 获取该音频的聊天历史（MySQL） |
| GET | `/api/sessions` | 历史会话列表 |
| GET | `/` | 返回前端页面 |

### 响应示例

```json
POST /api/audio
{ "id": "8f2a...", "status": "processing" }

GET /api/audio/{id}
{ "id": "8f2a...", "name": "季度会议", "status": "done",
  "duration_sec": 7.7, "created_at": "2026-08-01T14:20:00",
  "transcript": "大家好，今天我们来讨论...",
  "segments": [{"start":0.0,"end":3.2,"text":"大家好..."}] }

POST /api/audio/{id}/chat
{ "question": "会议的主要结论是什么？" }
→ { "answer": "根据音频内容，会议的主要结论是..." }

PATCH /api/audio/{id}  {"name": "季度会议"}
→ { "id": "8f2a...", "name": "季度会议" }
```

### 错误处理

- 文件类型不支持 → 400
- 音频过大 / 超时 → 413 或按后台任务失败处理
- 转写 / LLM 调用失败 → 返回结构化错误信息，前端提示重试

## 5. 数据结构

### 会话状态 `sessions/{id}.json`

```json
{
  "id": "8f2a...",
  "name": "季度会议",          // 会话名称, 可改名
  "filename": "meeting.m4a",
  "status": "done",            // processing | done | failed
  "progress": 1.0,             // 0.0 ~ 1.0
  "duration_sec": 7.7,         // 转写耗时(秒)
  "audio_path": "data/audio/2026-08/8f2a....m4a",
  "transcript_path": "data/transcripts/8f2a....md",
  "report_path": "data/reports/8f2a....md",
  "created_at": "2026-08-01T14:20:00"
}
```

> 说明：会话元数据存 JSON；问答历史存 MySQL（见下表），两者通过 `session_id` 关联。

### MySQL 表 `shenglu.chat_messages`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT PK AUTO_INCREMENT | 主键 |
| session_id | VARCHAR(32) | 会话 ID，索引 |
| role | VARCHAR(16) | `user` / `assistant` |
| content | MEDIUMTEXT | 消息内容 |
| created_at | DATETIME | 创建时间 |

## 6. 目录结构

```
ShengLu/
├── 需求/需求.md
├── 设计文档.md
├── README.md
├── requirements.txt
├── .env.example            # API Key 等环境变量示例
├── backend/
│   ├── app/
│   │   ├── main.py         # FastAPI 入口，静态文件挂载，MySQL 初始化
│   │   ├── config.py       # 配置（模型、DeepSeek、MySQL、目录）
│   │   ├── schemas.py      # Pydantic 请求/响应模型
│   │   ├── routers/
│   │   │   ├── audio.py    # 上传 / 状态 / 改名 / 下载接口
│   │   │   ├── report.py   # 报告接口
│   │   │   ├── chat.py     # 问答接口（历史存 MySQL）
│   │   │   └── sessions.py # 历史会话列表接口
│   │   └── services/
│   │       ├── asr.py      # Faster-Whisper 封装（繁体转简体）
│   │       ├── llm.py      # DeepSeek / OpenAI 兼容封装
│   │       ├── db.py       # MySQL 问答历史读写
│   │       └── storage.py  # 文件与会话管理（按月目录）
│   └── static/
│       ├── index.html
│       ├── app.js
│       └── style.css
├── data/                   # 运行时数据（gitignore）
│   ├── audio/YYYY-MM/      # 原始音频，按月子目录
│   ├── transcripts/
│   ├── reports/
│   └── sessions/
└── tests/
    └── test_api.py         # 接口测试（转写/报告/问答/历史/改名/下载）
```

## 7. 关键依赖

```txt
fastapi
uvicorn[standard]
faster-whisper
openai            # OpenAI 兼容客户端调用 DeepSeek
python-multipart  # 文件上传
pydantic
pymysql           # MySQL 问答历史
opencc-python-reimplemented  # 繁体转简体
pytest            # 测试
httpx
python-dotenv
```

> 系统级依赖：`ffmpeg`（音频解码）、可选 CUDA（GPU 加速）、MySQL 8.0（本机 3306）。
> 运行时说明：HuggingFace 模型下载走 `hf-mirror.com` 镜像并禁用 Xet；CUDA cuBLAS 通过 pip 包 `nvidia-cublas-cu12` 提供，config.py 自动加入 LD_LIBRARY_PATH。

## 8. 安全与隐私

- API Key 仅存于环境变量 / `.env`，不写入任何代码文件，`.env` 加入 `.gitignore`
- 音频与转写文本仅保存在本地 `data/` 目录，不主动外传
- 用户提问内容与音频内容一起发送至 LLM API 是必要的上下文传递；如涉及机密，可后续切换本地 LLM（Ollama）

## 9. 开发计划

| 阶段 | 内容 | 产出 | 状态 |
|------|------|------|------|
| M1 脚手架 | FastAPI 骨架、静态页、配置、目录结构 | 可启动的空服务 | ✅ |
| M2 ASR 转写 | 上传 + Faster-Whisper 转写 + 进度查询 | 音频 → 原文文件 | ✅ |
| M3 总结报告 | LLM 生成结构化报告 | 原文 → 报告文件 | ✅ |
| M4 智能问答 | 基于原文的聊天接口 + 前端聊天 UI | 提问 → 回答 | ✅ |
| M5 打磨 | 前端美化、错误处理、README | 完整可用版本 | ✅ |
| M6 历史与扩展 | MySQL 问答历史、按月存音频、历史列表/改名、耗时提示、md 下载 | 历史管理 + 持久化 | ✅ |

## 10. 后续可扩展

- 说话人分离（Speaker Diarization）识别不同发言者
- 会话删除 / 音频在线播放
- 本地 LLM 切换（Ollama）实现完全离线
- 批量音频处理
